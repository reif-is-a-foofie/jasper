# Jasper Core

`jasper-core/` contains Jasper-owned runtime primitives that should remain outside upstream Codex code.

Milestone 1 contents:

- identity configuration
- identity loader
- shared runtime metadata helpers
